# === INFRASTRUCTURE (required) ===
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/affiliate_tunnels
APP_ENCRYPTION_KEY=generate-a-64-char-hex-string-here
JWT_SECRET=generate-a-random-secret-here
JWT_REFRESH_SECRET=generate-another-random-secret-here
PORT=3001
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# === ALL OTHER CONFIG IS MANAGED VIA ADMIN SETTINGS UI ===
# Resend API Key        → Admin > Settings
# Stripe Keys           → Admin > Settings
# R2 Credentials        → Admin > Settings
# ClickBank Secret      → Admin > Settings
# GA4 / FB Pixel IDs    → Admin > Settings (or per-funnel)
# From Email / Name     → Admin > Settings
# Physical Address      → Admin > Settings (CAN-SPAM)
